const { Spinner } = wp.components;
import { Container } from '@wpbr/components';

export default () => {
    return (
        <Container textAlign="center">
            <Spinner/>
        </Container>
    );
}
